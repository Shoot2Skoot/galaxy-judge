/**
 * Game Configuration
 * Toggleable settings for development and gameplay modes
 */

export const gameConfig = {
  // Auto-reveal all evidence files immediately without requiring clicks
  // Useful for testing and faster gameplay
  autoRevealEvidence: true,
};
